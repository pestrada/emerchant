<%= form_for @order_item do |f| %>
  <%= f.error_messages %>
  <h2>
    <%= f.label @order_item.product.title %><br />
  </h2>
  <p>
    <%= f.label :quantity %><br />
    <%= f.number_field :quantity %>
  </p>
  <p><%= f.submit %></p>
<% end %>
